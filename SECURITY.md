# Security

Report security issues privately through GitHub's security advisory interface.
Do not include credentials or private endpoint data in reports, fixtures, or
logs.

The library accesses public market-data endpoints only. Consumers remain
responsible for dependency locking, release-attestation verification, and
their own trust policy.
